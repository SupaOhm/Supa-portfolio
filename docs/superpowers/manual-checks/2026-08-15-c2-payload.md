# Slice C2 manual checks — payload & fetch budget

> **Verification pass 2026-08-16** (Claude, Chrome against a local
> `vite preview` production build at commit `52aa9a7`). Boxes ticked below
> carry the evidence recorded here.
>
> - **Request dedupe confirmed over a real network stack.** Cold load with
>   cleared `sessionStorage`: exactly **2** `api.github.com` requests
>   (`/users/SupaOhm` + the repos endpoint) — one profile fetch serving both
>   `About` and `Connect`. Without the in-flight dedupe this would be 4.
> - **TTL confirmed:** reload within 10 minutes → **0** GitHub requests, stats
>   still rendered.
> - `sessionStorage` holds one `github-profile:SupaOhm` key carrying `savedAt`
>   plus a 14-field profile object.
> - **`loading="lazy"` genuinely defers**, which no test in the suite can prove:
>   on a true top-of-page load (`scrollY === 0`), **0** of 5 project images had
>   been fetched; after scrolling to `#projects`, **5**. All 5 carry
>   `loading="lazy"` and all are `.webp`.
> - `dist/images/projects/` contains **only** `.webp` (0 `.png`/`.jpg`), and
>   `assets-src/` is absent from the build output.
>
> **Not done:** real transfer sizes over the wire (the figures observed came
> from cache, so the ~278 KB and 6.4 KB `revrace` totals remain unconfirmed),
> the request-blocking failure modes, Safari private window, the >10-minute TTL
> expiry, and the StrictMode/navigation round trip.


Branch `perf/payload-budget`. None of these are provable by the test suite:
jsdom has no network stack, no layout engine, and no real sessionStorage.

## Images (DevTools > Network, disable cache, hard reload)

- [ ] Filter to Img. Total transferred for `/images/projects/*` is ~278 KB, not ~6.8 MB.
- [x] Every project image request is a `.webp`. No `.png` or `.jpg` under
      `/images/projects/` appears at all.
- [x] On first paint at the top of the page, project images have NOT been
      requested yet. They appear only as the Projects section scrolls into view.
      (This is the only real evidence `loading="lazy"` does anything.)
- [ ] `revrace.webp` transfers around 6.4 KB (6,604 bytes). A value near
      16 KB means the no-upscale clamp regressed.
- [ ] Every card renders an image. A broken/empty box means a `.webp` path in
      `projects.ts` does not match a file.
- [ ] Images still look acceptable at full width on a 2x display — quality 82
      at 800px wide shows no obvious artefacting.

## Images (deployed build)

- [x] Vercel preview: `dist` contains no `.png`/`.jpg` under
      `images/projects/`, confirming `assets-src/` is not deployed.

## GitHub requests (DevTools > Network, filter `api.github.com`)

- [x] Hard reload with an empty session: exactly **2** requests
      (`/users/SupaOhm` and the repos endpoint), not 4.
- [x] Reload again within 10 minutes: **0** requests. Stats still render in
      both the About and Connect sections.
- [x] Application > Session Storage contains one `github-profile:SupaOhm` key
      whose `savedAt` is a plausible epoch milliseconds value.
- [ ] Close the tab, open a new one, load the site: back to 2 requests,
      confirming the cache is session-scoped and not localStorage.
- [ ] Wait past 10 minutes with the tab open, then reload: 2 requests again.

## GitHub failure modes

- [ ] Block `api.github.com` in DevTools request blocking, then reload. Both
      sections show their loading/empty state. Nothing white-screens.
- [ ] Unblock and reload without clearing storage: stats return.
- [ ] Safari private window: the site loads and stats render, even though
      `sessionStorage.setItem` throws there.

## StrictMode / navigation

- [ ] `npm run dev`, load `/`, then navigate to `/about` and back. Stats stay
      populated in both sections — they never blank out. (This is the
      regression the no-AbortSignal design exists to prevent.)
- [ ] Dev console shows no React warning about setting state on an unmounted
      component.

## What no test in this branch proves

- The 278 KB figure is measured with `stat` on encoded files, not observed
  over the wire.
- `loading="lazy"` — jsdom implements no lazy loading. The test asserts only
  that the attribute is present. The DevTools check in this document is the
  sole evidence of real deferral.
- The 4 → 2 → 0 request progression — the cache tests use a fake `fetch` and
  injected fake storage. No test observes a real network request.
- Real `sessionStorage` is never exercised; storage is injected precisely so
  tests avoid it. Safari private-mode behaviour is manual-only.
- No test renders inside `<StrictMode>`. The hook's `let applies = true` flag
  exists specifically for React's dev-mode mount→cleanup→remount sequence,
  and review traced it as correct by construction — but no executable test
  exercises it. The "StrictMode / navigation" checks above are the only real
  evidence.
- The mid-flight-unmount jsdom test is a smoke test, not a guard. It was
  verified adversarially to pass both against an unwired hook and against a
  variant with a real per-consumer `AbortController` restored. The reason is
  structural: cancelling the shared request could only be introduced inside
  `getCachedGitHubProfile`, which takes no `AbortSignal` at all. The design
  is protected by an absent parameter, not by an assertion. If someone adds
  a signal parameter to that function, no test will stop them — which makes
  the navigation check above the real safety net.
- The shared-promise rejection path is untested for two concurrent
  consumers. Both `About` and `Connect` should show their empty state when
  the single shared fetch fails; that is covered by the "GitHub failure
  modes" checks above, not by the suite.
